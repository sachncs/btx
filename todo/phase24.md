# Phase 24 — Markdown / RST / JSON / GitHub updates

## Goal
Update all documentation, schema, and CI configuration files to use the new package name `btx`.

**Status**: COMPLETE — all tasks verified in the current tree.

## Notes
- Historical CHANGELOG entries are immutable records; do not retroactively rewrite.
- The end-of-phase grep leaves only intentional `bitcoin-cli`, `github.com/bitcoin/bips/...`, and the CHANGELOG's historical `bitcoin` mentions (documenting the rename).
